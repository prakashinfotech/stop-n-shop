# Default: normalize line endings to LF in the repo, convert on checkout per platform
* text=auto

# Shell scripts must always use LF (they run on Linux in Docker)
*.sh text eol=lf

# SQL files: LF in repo
*.sql text eol=lf

# Windows-only files can use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
