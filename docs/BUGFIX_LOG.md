# Bug Fix Log — `bug-fixes` branch

| # | Issue | Fix Summary | Files Changed |
|---|-------|-------------|---------------|
| 1 | **URL restructure** — routes were flat and ambiguous (`/login`, `/cart`, `/products/:id`) | Reorganised all routes under semantic prefixes: `/user/*` for buyer pages, `/home/*` for storefront, `/seller/register` instead of `/seller/signup`; added legacy redirects so old bookmarks still work | `AppRouter.tsx`, `Header.tsx`, `Footer.tsx`, `AuthModal.tsx`, `LoginPage.tsx`, `SignupPage.tsx`, `SellerLoginPage.tsx`, `OrderDetailPage.tsx`, `ProductDetailPage.tsx`, `NotFoundPage.tsx`, `axiosInstance.ts`, `AuthContext.tsx` |
| 2 | **Mobile OTP login broken** — "Could not send OTP" with no way to test without SMS | Disabled Twilio SMS sending; backend generates 6-digit OTP, saves to DB, returns it in response; frontend displays it in amber banner for 5 sec after Send/Resend; successful verify navigates to `/home` | `AuthService.cs`, `AuthController.cs`, `authApi.ts`, `AuthContext.tsx`, `LoginPage.tsx`, `AuthModal.tsx` |
| 4 | **Post-login empty screen** — two competing `navigate` calls (useEffect + handler) interrupted route transition; admin routing broke because `returnUrl === '/'` check used old default | Unified navigation into single `useEffect`; removed explicit navigates from `handleVerifyOtp` and `onEmailSubmit`; updated admin routing to `returnUrl ?? (isAdmin ? '/admin/dashboard' : '/home')` | `LoginPage.tsx` |
| 5 | **Forgot Password missing OTP step** — flow jumped directly from email to password reset with no verification | Added email OTP step: `ForgotPasswordLookupAsync` now generates + saves email OTP (type=1); new `POST /auth/forgot-password/verify-otp` endpoint; `ForgotPasswordModal` adds `otp` step with 60s resend timer, back navigation; OTP sent via email only (not shown in UI); SMTP configured for Office365; `SaveOtpAsync`/`VerifyOtpAsync` accept `otpType` param; fixed constructor param shadowing (`email` → `emailSvc`) | `AuthDtos.cs`, `AuthRepository.cs`, `AuthService.cs`, `AuthController.cs`, `authApi.ts`, `ForgotPasswordModal.tsx`, `appsettings.json` |
| 3 | **OTP login 500 error + no admin routing** — `VerifyOtpAsync` used `ExecuteAsync`, ignoring SP result and letting SQL THROW 50005–50008 bubble as unhandled exceptions; verify returned void so callers couldn't read role | `VerifyOtpAsync` now uses `QuerySingleOrDefaultAsync<int?>` with `catch SqlException when (50005–50008)`; `verifyOtp` in AuthContext returns `UserProfile`; LoginPage + AuthModal route buyer → `/home`, admin → `/admin/dashboard` | `AuthRepository.cs`, `AuthContext.tsx`, `LoginPage.tsx`, `AuthModal.tsx` |
| 6 | **Duplicate eye icons in password field** — browser's native password reveal button (Edge `::-ms-reveal`, Chrome) shown alongside custom Eye/EyeOff toggle | Suppressed browser reveal button with CSS pseudo-selectors `::-ms-reveal` and `::-ms-clear` in global stylesheet | `index.css` |
| 7 | **Duplicate signup button** — "Join Free" button appears alongside "Sign In" in header (desktop + mobile) | Removed redundant "Join Free" button from both desktop and mobile nav; "Sign In" is primary auth entry point | `Header.tsx` |
| 8 | **DB build script had wrong .sqlproj filename** — `BuildProject.bat` referenced `StopNShop.sqlproj`, actual file is `ShopNStopDB.sqlproj`, so build/deploy failed on a fresh checkout | Corrected the `SQLPROJ` variable in the batch | `ShopNStopDB/BuildProject.bat` |
| 9 | **Seed_CatalogSample.sql hardcoded `@AdminId = 3`** — broke on any fresh DB where the admin user isn't UserId 3, causing FK_Products_CreatedBy violation during seeding | Replaced hardcoded IDs with lookups: `@SellerId = TOP 1 SellerId`, `@AdminId = SELECT UserId WHERE Email = 'admin@stopnshop.com'` | `ShopNStopDB/dbo/Data/Seed_CatalogSample.sql` |
| 10 | **Seeded admin and demo seller passwords unusable** — admin hash was for an undocumented secret; demo seller had a placeholder bcrypt string that no hashing function could verify | Generated fresh PBKDF2-SHA256 hashes (100k iters, 16-byte salt, 32-byte hash, `{salt}:{hash}` base64 format) for `Test@123`; updated both seed files and live DB | `Seed_AdminUser.sql`, `Seed_DemoSeller.sql` |
| 11 | **Banner feature appeared lost** — banner CMS work (carousel, admin upload UI, new SPs, DTOs) lived only on `feature/banner-setting`; `bug-fixes` was cut from an earlier commit and didn't include it | Merged `feature/banner-setting` into `bug-fixes`, resolving 3 conflicts (kept the more-robust `TOP 1 SellerId` lookup in Seed_CatalogSample, kept the explanatory menuId comment in CategoryPage, accepted auto-merge for axiosInstance) | merge commit + `Seed_CatalogSample.sql`, `CategoryPage.tsx` |
| 12 | **Product detail page redirected to listing** — legacy redirect `/products/:id → /home/products` dropped the `:id` param; `ProductCard`, `CartPage`, `WishlistPage` all link with `/products/:id` so every click landed on the listing | Made `/products/:id` render `ProductDetailPage` directly (canonical `/home/product/:id` still works) | `AppRouter.tsx` |
| 13 | **Default product placeholder image broken** — `DEFAULT_PRODUCT_IMAGE` pointed at `/uploads/products/default-product.svg` which didn't exist, so every "no image" slot rendered alt text instead of a clean placeholder | Added an SVG placeholder under `api/wwwroot/uploads/products/default-product.svg` (served via static files + Vite proxy) | new file `api/wwwroot/uploads/products/default-product.svg` |
| 14 | **Seller product stock lost on save** — stock was only written onto variants; if seller didn't toggle colors or sizes, `BuildVariantCombos` returned empty and no variant was created, so the read-back sum was 0 | Create/Update flows now insert a single `V######-DEFAULT` variant when there are no color/size combos, carrying the entered stock | `SellerProductService.cs` |
| 15 | **Seller edit form showed product details as empty** — `GetDetailAsync` mapped only the core fields and explicitly discarded the SP's specifications result set; Material/Care/Fit/Country/Warranty/Delivery/Specifications were saved but never returned | Added those fields to `SellerProductDetailDto`, included them in the private `ProductRow`, mapped from SP output, and read the previously-discarded specs result set | `SellerDtos.cs`, `SellerProductRepository.cs` |
| 16 | **Newly added products didn't appear on home page** — `usp_Catalog_Product_Search` had no branch for `POPULAR`/`LATEST` sortBy; fell through to `ORDER BY ProductName ASC`, pushing new products past the 8-item home cap | Added `POPULAR`/`LATEST`/`Newest` → `ProductId DESC` ranking and changed the tie-break fallback from `ProductName ASC` to `ProductId DESC` | `usp_Catalog_Product_Search.sql` |
| 17 | **Discount badge missing on newly added products** — seller form didn't validate that MRP ≥ SellingPrice; entering reversed values produced a negative `discountPercent` and the badge hid itself (only shown when `> 0`) | Added "Selling price cannot be greater than MRP" guard in both Add and Edit forms | `SellerAddProductPage.tsx`, `SellerEditProductPage.tsx` |
| 18 | **Cart side drawer overlay** — cart actions opened a slide-in drawer instead of navigating; user wanted the dedicated cart page | Header cart button and Product Detail "Add to bag" success now `navigate('/user/cart')`; removed `<CartDrawer />` mount from router; dropped now-unused `useCartDrawer` imports/calls in Header and ProductDetailPage (provider/context retained, no consumers) | `AppRouter.tsx`, `Header.tsx`, `ProductDetailPage.tsx` |
| 19 | **Reviews looked like duplicates** — backend auto-derived `Title` from the first 60 chars of the comment, so a short review like "Great product!!" produced identical title and body; the UI rendered both lines so it appeared duplicated | Stopped auto-deriving the title in `EngagementService.AddReviewAsync`; title now stored as empty (UI already hides empty titles with `{review.title && ...}`); cleaned existing rows in DB where `Title = Body` | `EngagementService.cs` |
| 20 | **Cart page caused infinite redirect / didn't load** — when the buyer JWT was missing or expired, `/api/cart` returned 401, axios interceptor cleared tokens and bounced to login; with stale `sns_seller_token` still in localStorage it redirected to `/seller/login`, which on a fresh load re-fired the same logic | Rewrote the 401 interceptor: skip the logout/redirect when the failed request IS the login endpoint, and decide the login destination from the current URL path (`/seller/*` vs everything else) instead of from whichever token still happens to be in localStorage; clear only the tokens for the current area | `axiosInstance.ts` |
| 21 | **Wrong-creds on `/user/login` bounced to `/seller/login` (and vice-versa)** — same axios interceptor: a 401 on login itself was wiping state and routing the user to the opposite role's login page based on stale localStorage | Fixed alongside #20: the interceptor now early-returns on 401 from the login endpoints so failed login attempts surface as inline form errors without redirect, and any other 401 uses the current path to pick the right login page | `axiosInstance.ts` |
| 22 | **Login error flashed and disappeared** — the auth-endpoint regex from #21 only matched `/auth/login` and `/seller/auth/login`, but the real buyer endpoint is `/auth/buyer/login`. 401s from it still hit the redirect branch: state cleared, `window.location.href = '/user/login'` reloaded the page, and the inline `serverError` was wiped on remount | Widened the early-return to any URL under `/auth/` or `/seller/auth/` (login, OTP verify, forgot/reset password). Credential errors now surface as a persistent inline form error with no page reload | `axiosInstance.ts` |
| 23 | **Add-to-cart auto-navigated away from the product page** — on success the handler did `setTimeout(navigate('/user/cart'), 400)`, so the user lost their place in the PDP after the very first click | Drop the navigate-on-success. `onSuccess` now just sets `inCart=true` and shows an "Added to bag" toast. The button already swaps to "GO TO BAG"; clicking it again navigates to `/user/cart`. Also fixed the legacy `/cart` target in the in-cart click branch to `/user/cart` | `ProductDetailPage.tsx` |
| 24 | **Seller product form skipped the Menu level** — the form had only Category + Sub-Category dropdowns (flat list of all categories across all menus), so the seller couldn't choose MEN/WOMEN/KIDS/HOME/BEAUTY/WATCHES at the top, and the Sub-Category dropdown loaded values through a separate API call instead of the already-fetched menu tree | Reworked Add and Edit forms to drive the cascade from the mega-menu (`catalogueApi.getMegaMenu`). New Menu select on top resets downstream selections; Category options come from `selectedMenu.subCategories`; Sub-Category options come from `selectedCategory.productTypes`. Edit page infers the parent Menu from the loaded product's `categoryId` so dropdowns prefill correctly. Dropped the now-unused `loadSubCategories` API call | `SellerAddProductPage.tsx`, `SellerEditProductPage.tsx` |
| 25 | **3-level filter on Products page broken** — left-side filter had only 2 visible levels (Menu header + Category buttons), and clicking a Category sent its id as `subCategoryId` to the search API, which expected an actual `SubCategoryId`. Result: filter returned zero matches and the third tier (Sub-Categories) was never shown | Rebuilt the filter to render the full 3-level tree from the mega-menu: Menu link → Category buttons (set `categoryId`, clear `subCategoryId`) → Sub-Category buttons (set both, so the result is constrained to that subcategory within its parent category). Also fixed the menu link to the canonical `/home/category/<slug>` path | `ProductListPage.tsx` |
| 26 | **Storefront looked empty under most subcategories** — after the 3-level filter fix, clicking any subcategory other than the 6 covered by `Seed_CatalogSample.sql` rendered an empty list (men jeans/dresses/ethnic, men footwear ×3, kids baby-essentials, home furniture/kitchen, beauty ×3, watches/jewellery were all unseeded) | New idempotent seed `Seed_FullCatalog.sql` adds 3 products per gap subcategory (42 total) attached to the demo seller (`SellerId = 1`, `ApprovalStatus = 2`, `IsActive = 1`). Clones the structure of `Seed_CatalogSample.sql`: shared image pool, brand-by-name lookups with `COALESCE` fallback, `WHERE NOT EXISTS (SlugUrl)` idempotency guard, NEWID-based `FC-`-prefixed Sku, variants by SizeKind (apparel 2×3, footwear 1×4, home/beauty 2×0), 5 specifications. Re-run safe; orders on these products auto-attribute to the demo seller via `OrderItems.SellerId`, so seller and admin dashboards pick them up without code changes | new file `ShopNStopDB/dbo/Data/Seed_FullCatalog.sql` |
| 27 | **Every Menu (MEN / WOMEN / KIDS / HOME / BEAUTY / WATCHES) returned the same product list** — two compounding bugs: (a) `ProductListPage` never read `menuId` from the URL query and never passed it to the API, so the SP's `@MenuId` filter was always `NULL`; (b) all category tiles, "View all" links, MegaMenu items and PDP brand link pointed at the legacy `/products?...` route, which is a React Router `<Navigate>` that drops the query string when redirecting to `/home/products`. The combination meant clicks under MEN/WOMEN/etc. arrived at `/home/products` with no filter | Read `menuId` from `searchParams` in `ProductListPage` and include it in the API query. Rewrote every storefront link that targeted `/products?...` to use the canonical `/home/products?...` so the menu/category/subcategory params survive. MegaMenu links now carry full `menuId + categoryId + subCategoryId` triple for precise 3-level navigation | `ProductListPage.tsx`, `CategoryPage.tsx`, `ProductDetailPage.tsx`, `FeaturedProducts.tsx`, `MegaMenu.tsx` |
| 28 | **`POST /api/cart` returned 500 — "No available variant found for this product"** — the original `Seed_Products.sql` inserted 60 products into `Products` + `ProductImages` but never wrote a row into `ProductVariants`. The cart `AddToCartAsync` requires a `VariantId` (cart/order schema is variant-keyed), so any add-to-bag on those 60 products threw `InvalidOperationException` and the global exception middleware returned 500 | One-shot backfill against the live DB: `INSERT ProductVariants` a single `V######-DEFAULT` row (Color=`Default`, no Size, Stock=50, LowStock=5, IsActive=1) for every product with zero active variants — 60 rows inserted. Also patched `Seed_Products.sql` to run the same backfill at the end so fresh deploys don't reintroduce the gap. Now matches the convention from the seller-side service fix (#14) where products without colors/sizes get a single default variant | live DB + `Seed_Products.sql` |
| 29 | **Cart UX felt utilitarian and coupons weren't discoverable** — the coupon input was hidden inside the Payment step (step 2 of 3); a buyer scanning the Bag didn't know discounts were possible until they'd already filled in an address. Empty-state "Continue Shopping" link pointed at the legacy `/products` route. Order success was just a spinner → toast → silent navigation with no acknowledgement | Added a discoverable coupon card to the Bag step (step 0) in the existing brand palette (amber gradient + brand-red CTA): input + Apply button, inline error display, and clickable suggestion chips (WELCOME10 / SAVE50 / FESTIVE15) that pre-fill the field. Replaced the silent post-order navigation with a full-screen celebratory overlay (24 confetti particles in brand red, gold, emerald, and indigo; spring-scaled green check; order ID + amount paid card; "Keep Shopping" + "View Order" CTAs). Fixed the empty-state link target to `/home/products` and the order-detail navigation to the canonical `/user/orders/:id` | `CartPage.tsx` |
| 30 | **Coupon UX needed a polished Myntra-style layout + admin lifecycle controls** — the previous amber card in step 0 was discoverable but bulky and existed in two duplicate copies (bag step and payment step). Admins had no way to add or disable coupons — every coupon was hand-seeded via raw SQL — and there was no brand-specific coupon support exposed | (1) Cart: collapsed the two duplicate coupon blocks into one compact "COUPONS" card sitting in the sticky right-rail summary, with a single-row input + APPLY tap-target, inline error text, and small suggestion chips below — matches the Myntra reference visually while keeping the existing brand palette. (2) Seeded 4 realistic coupons into the live DB (`WELCOME10` 10% off, `SAVE50` flat ₹50, `FESTIVE15` 15% off, and `NIKE20` 20% off Nike-only — first brand-specific coupon using `Offers.ApplicableOn = 2 + EntityId`). (3) Built admin Coupons module: 3 new SPs (`usp_Admin_Coupon_GetAll`, `usp_Admin_Coupon_Create`, `usp_Admin_Coupon_Toggle`), `ICouponRepository` / `ICouponService` extensions, three admin endpoints (`GET /admin/coupons`, `POST /admin/coupons`, `PATCH /admin/coupons/:id/toggle`), and `AdminCouponsPage.tsx` with a table view + inline create form (type, %/₹ value, min/max, dates, applies-to = entire cart or specific brand). Disabling a coupon also disables its parent Offer so it can't leak through other surfaces; coupons stay active until an admin disables them | new SPs in `ShopNStopDB/dbo/StoredProcedures/`, `CouponDtos.cs`, `CouponRepository.cs`, `CouponService.cs`, `AdminController.cs`, `couponsApi.ts`, `AdminLayout.tsx`, `AppRouter.tsx`, new `AdminCouponsPage.tsx`, `CartPage.tsx` |
| 31 | **Order detail page: tracking didn't highlight + price details ignored coupons** — the Order Status in DB is a `TINYINT` (1=Pending, 2=Confirmed, …) but the repository did `raw.OrderStatus.ToString()` which yielded `"1"` / `"2"`. The UI tracking timeline matches on string labels (`"PLACED"`, `"CONFIRMED"`, …) so it never matched and the current step never got the green ring. Separately, `OrderDetailDto.Summary` exposed only `totalMRP / totalDiscount / deliveryCharge / finalAmount`; `CouponDiscount` and `CouponCode` were populated in the SP result but never read into the DTO. And `placeOrder` shipped `CouponCode = null` to the SP so even when the buyer had applied a coupon in the cart, it wasn't persisted with the order | Backend: added `MapOrderStatus(byte)` translating the schema's 1–7 ints to `PLACED/CONFIRMED/PACKED/DISPATCHED/DELIVERED/CANCELLED/RETURNED`, used by both `GetOrderDetailAsync` and `GetOrdersAsync`. Read `SubTotal`, `DiscountAmount`, `CouponDiscount`, `CouponCode`, `ShippingCharge`, `TotalAmount` separately into the DTO and exposed `CouponDiscount` + `CouponCode` on `OrderSummaryDto`. Threaded `CouponCode?` through `PlaceOrderRequest` → `OrderService.PlaceOrderAsync` → repo → `usp_Commerce_Order_Place` so the SP computes and persists `CouponId` / `CouponDiscount` on the row. Frontend: cart `placeOrderMutation` now sends `appliedCoupon?.couponCode`; `OrderDetailPage` Price Details now shows Total MRP (with item count), Discount on MRP (conditional), Coupon (conditional with code), Delivery (FREE highlight), Total, and a "You saved ₹X" line — same shape as the cart summary. Fixed the "Continue Shopping" link from legacy `/products` to `/home/products` | `CartOrderRepository.cs`, `CartOrderDtos.cs`, `CartOrderService.cs`, `cart.types.ts`, `CartPage.tsx`, `OrderDetailPage.tsx` |
| 32 | **Admin module empty across the board** — dashboard, sellers, products, users, orders, reviews all showed nothing because every list endpoint returned `404`. The UI shipped `adminApi.*.getAll` clients but the API only had `GET /admin/products/pending` plus CMS/coupon routes; everything else was unimplemented. Three of the underlying read SPs (`Admin_Seller_GetAll`, `Admin_Product_GetAll`, `Admin_Review_GetAll`) didn't exist. The legacy `HttpPut("products/{id}/approve")` and `RejectProduct` methods existed but used the wrong HTTP verb and the wrong SP param name (`@RejectionReason` vs the actual `@RejectReason`), so even the products screen's approve/reject would have errored | Wrote the 3 missing SPs (paginated, search-aware) and applied to live DB. Added `AdminDtos.cs` (stats, seller, product, user, order, review row shapes + `RejectReasonRequest`). New `AdminRepository` / `IAdminRepository` bundles every admin read + moderation action (approve/reject/suspend) and translates the numeric `OrderStatus` byte to the string labels the UI tracker expects. Registered the repo in DI; injected into `AdminController`. Removed the two broken legacy product routes; added `HttpPatch` siblings matching the UI client and identical sibling patches for sellers (approve/reject/suspend) and reviews (approve). Listings paginate via a small `Paged()` helper. All 5 endpoints (dashboard / sellers / products / users / orders) now return real rows; reviews ready for the next time someone writes one | 3 new SPs in `ShopNStopDB/dbo/StoredProcedures/usp_Admin_*_GetAll.sql`, `api/DTOs/AdminDtos.cs`, `api/Repositories/AdminRepository.cs`, `api/Controllers/AdminController.cs`, `api/Program.cs` |
| 33 | **Broken product images everywhere** — 133 of 193 `ProductImages` rows pointed at `/uploads/products/*.png` paths that referenced 14 distinct files, but only 2 of those files actually existed in `api/wwwroot/uploads/products/`. Every product card built from `Seed_CatalogSample` or `Seed_FullCatalog` was rendering the "No image" placeholder | Uploaded 14 themed photos (apparel, footwear, beauty, jewellery, home, etc.) to ImgBB via the existing `ImgBB:ApiKey` in `appsettings.json`. Built a SQL update mapping each broken local path → its ImgBB `display_url` and ran it in one shot (133 rows updated). Replaced the local-path image pool inside `Seed_CatalogSample.sql` and `Seed_FullCatalog.sql` with the same 13 ImgBB URLs so fresh deploys come up populated. Storefront now shows real photos across all categories. After: 0 broken-local rows, 133 ImgBB, 60 Unsplash (already-working) | live DB + `Seed_CatalogSample.sql`, `Seed_FullCatalog.sql` |
| 34 | **Admin Preview Mode** — admins logging into the storefront need a read-only buyer experience so they can sanity-check the UI without polluting their account with cart/order/wishlist data | Added `usePreviewMode()` hook (single source of truth for `role === 'Admin'`). Header now hides cart icon, wishlist link, and `<NotificationBell />` for admins, and the account dropdown collapses to "Back to Dashboard" + "Sign Out" (desktop and mobile). `ProtectedWishlistButton` no-ops + renders disabled with tooltip in preview. `ProductDetailPage` disables the heart and the Add-to-Bag button (both top section and sticky footer), and the "GO TO BAG" state is suppressed for admins. `CartPage` no longer calls `cartApi.getCart` for admins (query `enabled: !previewMode`); admins land on a banner ("Admin preview — checkout disabled" + "BACK TO DASHBOARD") and an empty-state card explaining the read-only mode. Router: `CustomerRoute` now redirects authenticated Admins to `/admin/dashboard` instead of asking them to "log in as buyer" for the other personal pages; a new `BuyerOrAdminRoute` wraps `/user/cart` so admins can reach the preview. No API changes — UI-only feature. Buyer and Seller flows untouched | new `hooks/usePreviewMode.ts`, `Header.tsx`, `ProtectedWishlistButton.tsx`, `ProductDetailPage.tsx`, `CartPage.tsx`, `AppRouter.tsx` |
| 35 | **Wrong-role navigation could loop or dump users on a confusing login screen** — `SellerRoute` redirected any non-Seller (including authenticated buyers) to `/seller/login`, and `AdminRoute` did the same toward `/user/login` — even if the visitor was already authenticated as a buyer. With a valid token in localStorage, the buyer would land on a login page they couldn't use, with no signpost to where they should be | Made both guards role-aware with three branches: (1) not authenticated → original role-specific login with `returnUrl`; (2) authenticated but wrong role → redirect to `/home` (a public, gate-free landing); (3) right role → render children. Added a tiny `WrongRoleBounce` component that fires a single error toast ("That area is for sellers/admins only.") via `useEffect` before the `<Navigate />`, so the user understands the bounce instead of feeling kicked out silently. No risk of loop because `/home` is gate-free. `CustomerRoute` already had the symmetric behaviour (admin → `/admin/dashboard`) so the pattern is consistent across all three role guards | `stopnshop-ui/src/router/AppRouter.tsx` |
| 36 | **Already-authenticated users could still open the other role's login page** — a signed-in buyer manually typing `/seller/login` saw the seller login form (which is misleading at best, and at worst encourages them to start a parallel seller session). Same for buyers opening `/seller/register`, sellers opening `/user/login`, etc | Added a `GuestRoute` wrapper that gates each login/signup page. It renders the page only when unauthenticated; for an already-authenticated visitor it picks the right landing: same-role visitor → their own dashboard, wrong-role visitor → `/home` (buyer/admin trying seller pages) or the right dashboard (admin/seller trying buyer pages), with a one-shot toast explaining the bounce. Wrapped `/user/login`, `/user/signup`, `/seller/login`, `/seller/register`. Pattern matches #35's `WrongRoleBounce`; no API changes; `/home` is the gate-free terminator so no loop risk | `stopnshop-ui/src/router/AppRouter.tsx` |
| 37 | **No portable way to clone the working DB onto another machine** — every fresh deploy started from empty seed scripts, so a teammate (or your office laptop) couldn't reproduce your local state (uploaded products, applied coupons, seed catalog, etc.) without manually re-running every workflow | Shipped two paths and committed both side-by-side under `db-export/`. (A) **BACPAC**: `scripts/db-export.bat` runs `sqlpackage /Action:Export` to produce a 124 KB schema-plus-data binary; `scripts/db-restore.bat` runs `sqlpackage /Action:Import` on the target. One file, one command per side, full fidelity. (B) **Seed_DataDump.sql**: `scripts/generate-seed-dump.sql` is a SQL-side data scripter that creates a temporary `usp_Tool_GenerateInserts` proc, walks 22 tables in FK-safe order (Users early so every `CreatedBy/UpdatedBy` FK resolves), and prints typed `INSERT` statements with `IDENTITY_INSERT` toggles and `DELETE FROM` guards. Run via `sqlcmd -y 0 -o` to capture 1167 inserts (~525 KB). Idempotent — re-running lands on the same state. Test-replayed onto a fresh sandbox DB built from a freshly extracted DACPAC; final row counts match the source exactly (Brands 15, Products 127, ProductImages 193, Coupons 5, Orders 1). `db-export/README.md` documents both flows; `appsettings.json` secrets explicitly out of scope. | new `scripts/db-export.bat`, `scripts/db-restore.bat`, `scripts/generate-seed-dump.sql`, `db-export/ShopNShop_db.bacpac`, `db-export/Seed_DataDump.sql`, `db-export/README.md` |
