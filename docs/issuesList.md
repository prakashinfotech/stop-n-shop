# Issues List

| # | Description | Date | Status |
|---|-------------|------|--------|
| 3 | OTP login gives 500 error. Phone 8141294082 exists as buyer. Need: check buyer+admin, generate OTP, display on screen, return isAdmin flag in verify response, route buyer → /user/home, admin → /admin/dashboard | 2026-05-14 | Resolved |
| 4 | Empty screen after OTP login success — navigate lands on `/home` but page renders blank | 2026-05-14 | Resolved |
| 5 | Forgot Password: email OTP flow missing — validate buyer/admin email, generate 6-digit OTP, show on screen, verify OTP, then allow password update; close dialog on success | 2026-05-14 | Resolved |
| 6 | Duplicate eye icons in password field — browser's native reveal button shown alongside custom Eye/EyeOff toggle | 2026-05-14 | Resolved |
| 7 | Duplicate feature: "Join Free" button appears alongside "Sign In" — removed redundant signup button | 2026-05-14 | Resolved |
| 8 | Unable to login using admin credentials (admin@stopnshop.com / Admin@StopNShop2026) — returns 401 Unauthorized | 2026-05-14 | Investigating |
| 9 | Sessions don't expire on the client — returning after a long absence shows the previously logged-in user instead of routing to login. Backend JWT expires (30 days) but frontend never decodes the `exp` claim; `loadInitialState()` blindly trusts whatever token is in localStorage until something makes an API call and gets a 401. Fix: shorten token lifetime to 8h + check JWT expiry on app load + on window focus/visibilitychange, clear state when expired. | 2026-05-20 | Resolved |
| 10 | Buyer does not receive cancellation email when seller cancels an order. Root cause: the fire-and-forget block in `SellerOrderController.CancelOrder` wraps the SMTP call in `try { ... } catch { /* never block */ }` with no logging, so any failure (null profile, empty email, SMTP error) disappears silently. Fix: injected `ILogger`, log a warning if buyer email is empty + log an error with the exception if SMTP throws, so the failure is visible in API logs next time. | 2026-05-20 | Investigating |
| 11 | Seller Inventory page shows MY SKUS=0, WAREHOUSES=0, "No stock rows", but LOW STOCK=380. Three root causes: (a) `/api/inventory/matrix` is `[Authorize(Roles = "Admin")]` — sellers get 403 on the main list call; (b) controller never derives SellerId from the JWT for seller calls, so even if matrix opened to sellers it would expose every seller's stock cross-tenant; (c) Docker DB has zero Warehouses and zero Stock rows — `Seed_Warehouses.sql` exists but `Script.PostDeployment.sql` has all `:r` lines commented out and `db/seed.sql` doesn't include warehouses either. Fix path: open matrix to Seller role + force-scope SellerId from JWT when caller is Seller; seed default warehouses via patch script; auto-create Stock(0,0) rows for variants on a default warehouse so the matrix returns rows for sellers to adjust. | 2026-05-26 | Open |
